import React from 'react'
import {
  Grid,
  Row,
  Col,
  Jumbotron,
} from 'react-bootstrap';

// class

import './ProgressBar.css';

const ProgressBar = () => (
          <div id="progress">
            <div id="bar"></div>
          </div>
);

export default ProgressBar
